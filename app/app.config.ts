export default defineAppConfig({
  ui: {
    colors: {
      primary: 'navy',
      secondary: 'blush',
      // Route default/neutral chrome through primary so grays aren't used
      neutral: 'navy'
    }
  }
})
